import { View, Text } from 'react-native';

export default function QuizListScreen() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <Text>퀴즈 목록</Text>
    </View>
  );
}
